<!-- Creating comment form -->
<%= form_with(model: [ @article, @article.comments.build ]) do |form| %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>

    <%= form.submit %>
  </p>
<% end %>